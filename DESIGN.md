# Airusy — DESIGN.md

## Палитра

Взята **только по цветам** с референс-скрина (без использования JPG на сайте):

| HEX | Роль |
|-----|------|
| `#101314` | Фон |
| `#1B434A` | Поверхности |
| `#426D7C` | Вторичный |
| `#C8EEE4` | Акцент |
| `#D2E4E4` | Текст |

## Визуал hero

**Не** вставляем скриншот. Оригинальная сцена:

- CSS mesh-градиент
- Анимированные световые полосы (beam)
- Canvas — пульсирующая сеть узлов
- Две CSS-карточки с 3D-наклоном, точечным паттерном и бликом
- Pill-вкладки как на референсе

## Анимации

- `beam-sweep` — движение света
- `card-float` / `card-shine` — «обложки»
- `orb-drift` — фоновое свечение
- Canvas — сеть узлов
- Parallax карточек по курсору

`prefers-reduced-motion` отключает анимации.
